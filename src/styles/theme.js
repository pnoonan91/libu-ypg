import {
  breakpoints,
  breakpointMappings,
  elementSizes,
  fontSizes
} from "./sizes";
import { fonts } from "./fonts";
import { colors } from "./colors";
import { shadows } from "./box-shadows";

const theme = {
  breakpoints,
  breakpointMappings,
  elementSizes,
  fontSizes,
  fonts,
  colors,
  shadows
};

export default theme;
